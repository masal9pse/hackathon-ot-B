version: '3'

services:
    container_name: node-chatapp
    node-chatapp:
        build:
            context: ./
            dockerfile: Dockerfile.dev
        ports:
            - 3000:3000
        volumes:
            - "./:/app"
            - "/app/node_modules"
